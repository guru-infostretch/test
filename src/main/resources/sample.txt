There is nice way to do this is using IOUtils to convert the InputStream into a Byte Array ... InputStream is; byte[] bytes = IOUtils.toByteArray(is); Here you can use Base64 to convert Byte Array to String . String encoded = Base64.getEncoder().encodeToString(bytes);
There is nice way to do this is using IOUtils to convert the InputStream into a Byte Array ... InputStream is; byte[] bytes = IOUtils.toByteArray(is); Here you can use Base64 to convert Byte Array to String . String encoded = Base64.getEncoder().encodeToString(bytes);
There is nice way to do this is using IOUtils to convert the InputStream into a Byte Array ... InputStream is; byte[] bytes = IOUtils.toByteArray(is); Here you can use Base64 to convert Byte Array to String . String encoded = Base64.getEncoder().encodeToString(bytes);
There is nice way to do this is using IOUtils to convert the InputStream into a Byte Array ... InputStream is; byte[] bytes = IOUtils.toByteArray(is); Here you can use Base64 to convert Byte Array to String . String encoded = Base64.getEncoder().encodeToString(bytes);
There is nice way to do this is using IOUtils to convert the InputStream into a Byte Array ... InputStream is; byte[] bytes = IOUtils.toByteArray(is); Here you can use Base64 to convert Byte Array to String . String encoded = Base64.getEncoder().encodeToString(bytes);
There is nice way to do this is using IOUtils to convert the InputStream into a Byte Array ... InputStream is; byte[] bytes = IOUtils.toByteArray(is); Here you can use Base64 to convert Byte Array to String . String encoded = Base64.getEncoder().encodeToString(bytes);
